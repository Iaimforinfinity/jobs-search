project:
  name: Job Search Agent
  description: AI agent to analyze Google job listings and suggest matches

agents:
  - id: job-matcher
    path: agents/job-matcher.agent.yaml

workflows:
  - id: job-filter-workflow
    path: workflows/job-filter.workflow.yaml
