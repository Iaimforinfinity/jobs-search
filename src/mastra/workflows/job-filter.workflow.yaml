id: job-filter-workflow
type: workflow
description: Filters job listings using the Job Matcher agent

steps:
  - id: evaluate_job
    uses: job-matcher
    with:
      job_title: ${inputs.job_title}
      job_description: ${inputs.job_description}

outputs:
  match_result: ${steps.evaluate_job.match_result}
